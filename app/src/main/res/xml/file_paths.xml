<?xml version="1.0" encoding="utf-8"?>
<!-- Item 8: only exposes the one cache subfolder the camera-notch button's
     capture flow actually writes into (see MainActivity's cameraCaptureUri
     helper) — nothing else in the app's storage is reachable through this
     provider. -->
<paths>
    <cache-path name="camera_capture" path="camera_capture/" />
</paths>
